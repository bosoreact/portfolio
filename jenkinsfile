pipeline {
    agent any
    tools {
        nodejs 'Node.js 16.20.0' // Specify the desired Node.js version
    }
    stages {
        stage('Check PATH') {
            steps {
                script {
                    echo "PATH: ${env.PATH}"
                }
            }
        }
        stage('Build') {
            steps {
                nodejs('node installer 16.20') {
                    sh 'npm install'
                    sh 'npm run build'
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}